import { SellRequestStatus } from './sell-request-status';

describe('SellRequestStatus', () => {
  it('should create an instance', () => {
    expect(new SellRequestStatus()).toBeTruthy();
  });
});
